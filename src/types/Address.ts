export type BlockAddress = {
  searchVal: string;
  blkNo: string;
  roadName: string;
  building: string; // e.g., "NIL" or building name
  address: string;
  postal: string;
  x: string; // SVY21 X coordinate
  y: string; // SVY21 Y coordinate
  latitude: string; // WGS84 latitude
  longitude: string; // WGS84 longitude
};
